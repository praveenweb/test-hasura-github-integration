- name: allowed-queries
  definition:
    queries:
    - name: fetchOneProduct
      query: |-
        query fetchOneProduct {
          products(limit: 1, order_by: {created_at: desc}) {
            id
            name
          }
        }
  comment: Managed by pro hasura
